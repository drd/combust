TODO
- calculate x coordinates by time rather than slot
  - look at total duration
  - change state.xScale from pixel width to amount of time visible
  - diff samples in preprocessing to hint renderer
- move hover to separate canvas
- replace magic numbers
  - padding
  - hsl constants
  - stroke & text thresholds
  - width & height calculations